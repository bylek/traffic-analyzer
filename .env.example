# Port HTTP
PORT=3000

# Dane logowania do panelu admina (Basic Auth) — WYMAGANE
ADMIN_USER=admin
ADMIN_PASS=zmien-to-haslo

# Ścieżka do pliku bazy SQLite
DB_PATH=./traffic.db

# Nagłówek, z którego brane jest realne IP klienta (apka stoi za reverse proxy)
IP_HEADER=x-forwarded-for
